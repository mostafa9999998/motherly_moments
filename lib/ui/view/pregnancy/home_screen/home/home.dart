import 'package:flutter/cupertino.dart';

class Weekscalculate extends StatelessWidget {
  const Weekscalculate({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color(0xff131baf),

    );
  }
}
