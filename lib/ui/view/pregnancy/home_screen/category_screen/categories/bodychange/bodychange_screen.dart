import 'package:flutter/material.dart';

class Bodychange extends StatelessWidget {
  const Bodychange({super.key});
  static String bodychangename = 'body';

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}